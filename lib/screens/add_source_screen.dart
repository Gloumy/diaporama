import 'package:flutter/material.dart';

class AddSourceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("add source")),
    );
  }
}
